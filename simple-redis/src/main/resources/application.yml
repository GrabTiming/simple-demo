server:
  port: 8080

spring:
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8 # 最大连接数
        max-idle: 8 # 最大空闲连接数
        max-wait: 1ms # 最大阻塞等待时间，-1为没有限制，上面同理
    timeout: 1000ms # 连接超时时间
#    password: 123456